import { Container } from "react-bootstrap"

const Home = () => {
    return <Container style={{minHeight: '100vh'}}>
        <h1>Home page</h1>
        <p>Will be ready in some time</p>
    </Container>
}

export default Home;